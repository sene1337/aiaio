{
  "name": "aiaio",
  "private": true,
  "version": "1.0.0",
  "description": "AIAIO — Agents In Amnesia, Insane Ordnance. Turn-based artillery inside an AI-agent session.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "extract": "node scripts/extract-sessioncard.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
